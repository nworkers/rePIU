# GS byte-load 작업 로그

공용 ModR/M decoder 기반 `65 8A /r` 처리를 추가했습니다. root가 nonzero candidate 단계에 도달하지 않아 byte name 비교는 아직 실행되지 않은 것으로 확인됐습니다.

# GS Byte-Load Work Log

Added shared-decoder `65 8A /r` handling. The root remains zero before byte name comparison is reached.
