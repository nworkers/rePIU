# DOS IOCTL HLE 작업 로그

## 결과

`piu_1st`의 `INT 21h AH=0x44 AL=0x00` 요청을 traced DOS HLE에서 처리했다. 관측된 요청은 handle `1`의 device information 조회였고, 표준 handle을 character device로 보고 `DX=0x0080`, CF clear로 응답한다.

loader 실행 로그에는 마지막 IOCTL subfunction, handle, 결과, device info를 출력하도록 추가했다.

## 검증

* `cmd /c scripts\build_win32_x86.bat`: 성공
* `build\win32_x86_debug\Debug\repiu_loader_win32.exe piu_1st`: 이전 `0x020F96E1` IOCTL 지점을 통과

# DOS IOCTL HLE Work Log

## Result

Handled `INT 21h AH=0x44 AL=0x00` in the traced DOS HLE path for `piu_1st`. The observed request queries device information for handle `1`; standard handles are treated as character devices and return `DX=0x0080` with CF clear.

The loader now prints the last IOCTL subfunction, handle, result, and device info.

## Verification

* `cmd /c scripts\build_win32_x86.bat`: passed
* `build\win32_x86_debug\Debug\repiu_loader_win32.exe piu_1st`: passed the previous IOCTL point at `0x020F96E1`
