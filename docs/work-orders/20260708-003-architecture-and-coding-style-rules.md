# 작업 지시: 아키텍처 및 코딩 스타일 규칙 추가

## 목표

현재 구현되는 코드의 설계와 구조를 `ARCHITECTURE.md`에 지속적으로 업데이트한다는 규칙을 추가하고, 사용자가 제공한 C++ 코딩 스타일을 `docs/CODING_STYLE.md`에 반영한다.

## 범위

* `AGENTS.md` 참조 문서 및 문서 작성 규칙 갱신
* 루트 `ARCHITECTURE.md` 추가
* `docs/CODING_STYLE.md` 갱신
* 설계 문서, 작업 지시 문서, 작업 로그 문서 추가

## 검증

* 추가/갱신 문서가 존재하는지 확인한다.
* Git diff로 의도한 문서 변경만 포함되는지 확인한다.

## Work Order: Architecture and Coding Style Rules

## Goal

Add the rule that the design and structure of currently implemented code must be continuously updated in `ARCHITECTURE.md`, and reflect the C++ coding style provided by the user in `docs/CODING_STYLE.md`.

## Scope

* Update `AGENTS.md` reference documents and documentation rules.
* Add root `ARCHITECTURE.md`.
* Update `docs/CODING_STYLE.md`.
* Add design, work-order, and work-log documents.

## Verification

* Confirm that added and updated documents exist.
* Confirm with Git diff that only intended documentation changes are included.
