# LINEXE root memory 관찰 작업 지시

실행 결과와 로그에 `0020h` descriptor 및 root word를 추가하고 빌드·실행으로 PIU scan과 비교합니다.

# LINEXE Root-Memory Observation Work Order

Add descriptor/root values to execution results and compare them with PIU scan behavior.
