# LINEXE export 해석 관찰 작업 지시

실행 결과 구조에 selector word와 8개 export 값을 추가하고 loader 로그로 출력합니다. 빌드 및 장시간 PIU 실행으로 fatal 시점 값을 확인하고 분석 문서에 반영합니다.

# LINEXE Resolution Observation Work Order

Add selector/export snapshots to execution results and loader logs, then build and capture their values at the fatal frontier.
