# GS word-load 작업 지시

FS 전용 word reader/decoder를 FS와 GS 공용으로 확장하고 saved root 및 export slot을 실행 관찰로 검증합니다.

# GS Word-Load Work Order

Generalize the FS word reader/decoder to FS and GS, then observe root and export resolution.
