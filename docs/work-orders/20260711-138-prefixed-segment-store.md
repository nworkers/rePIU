# prefix segment store 작업 지시

`66 8C /r` register form을 segment-store HLE에 추가하고 빌드 후 saved GS와 export slot 변화를 장시간 실행으로 확인합니다.

# Prefixed Segment Store Work Order

Add register-form `66 8C /r`, build, then observe saved GS and resolved export slots.
