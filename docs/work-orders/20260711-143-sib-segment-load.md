# SIB segment-load 작업 지시

memory-form `8E /r`을 공용 주소 decoder로 확장하고 module-name/export 해석을 실행 검증합니다.

# SIB Segment-Load Work Order

Extend memory-form `8E /r` with the shared decoder and verify module/export resolution.
