# AOT RET dispatcher safety work order

## ??

1. AOT code-cache emitter?? RET inline-cache fast-path? ???????.
2. RET? breakpoint dispatcher? ?? ?? `HandleAotReturnTransfer`? guest ESP? ???? ???.
3. Win32 x86 Debug ?? ? `aot-dynamic` 180? ???? `0x0304ED35` zero-EIP ?? ??? ?????.
4. ?? trap backend? Glide ordinal ? ?? ??? ?????.

## ?? ??

`aot-dynamic`? RET ?? `EIP=0`?? ???? ??, ?? 180? supervisor ??? ???? ???.

# AOT RET Dispatcher Safety Work Order

## Work

Disable the AOT RET inline-cache fast-path, route RET through the existing breakpoint dispatcher,
build Win32 x86 Debug, and run `aot-dynamic` for 180 seconds. The run must not terminate from

