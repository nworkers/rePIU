# LINEXE procedure-resolution frontier work order

virtual Glide `GETPROCADDR`의 미해결 요청 이름을 기록하고 장시간 실행으로 raw resolver
fallback과의 관계를 확인합니다.

Record unresolved virtual-Glide `GETPROCADDR` names and use a long run to establish their
relationship to the raw resolver fallback.
