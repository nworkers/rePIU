# ?? ??: GLSL shader `glGetError` ?? ?? / Work order: complete the GLSL shader `glGetError` policy

Task 377. ??: [20260801-377](../design/20260801-377-shader-gl-error-check-completion.md)

## ???

`GlideOpenGlShader`? fog mode/color/table ? alpha/color combine? ?? ?? `glGetError()`? ?? `REPIU_GLIDE_GL_ERROR_CHECK` ???? ????. ?? OFF??? GL ?? ?? ?? ??? ????, `1`?? ?? ?? ??? ?? ???? ?????.

??: ??/ON ?? ?? ??? Win32 Release ??? ????, ?? FPS ?? gameplay ???? swap interval 0 ? ordinal profile A/B? ?????.

## English

Gate the five remaining `glGetError()` calls in `GlideOpenGlShader` ? fog mode/color/table and alpha/color combine ? with the existing `REPIU_GLIDE_GL_ERROR_CHECK` policy. Default OFF performs no GL query and returns success; `1` preserves the previous checks and messages.
