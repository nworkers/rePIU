# LINEXE module name 관찰 작업 지시

module descriptor/record/name과 GS byte-load trace를 실행 결과에 추가하고 빌드·장시간 실행으로 비교합니다.

# LINEXE Module-Name Observation Work Order

Add module descriptor/record/name and GS byte-load trace to execution observations, then build and compare.
