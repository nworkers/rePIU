# GS byte-load 작업 지시

FS/GS `8A /r` byte load를 공용 decoder에 연결하고 export 해석 개수를 실행 검증합니다.

# GS Byte-Load Work Order

Connect FS/GS `8A /r` byte loads to the shared decoder and verify export resolution.
