# Task 434: the sample history keeps JSON only. The readable report is a
# release-workflow artifact now, and each snapshot committed here added tens of
# megabytes to the repository permanently.
tests/history/openwatcom_samples/*.html
build/
MASTER/
roms/
tools/openwatcom/
tools/downloads/
samples/*/build/
*.user
*.suo
*.tmp
*.obj
WIP/
eeprom.dat
# Loader diagnostic output: whole-run stderr captures and Glide census logs.
# Megabytes per run and regenerated on demand, so they stay out of history.
repiu_*.txt
*-capture.log
smoke-*.log
musicselect*.log
tex*.log
# A/B captures named by the elision testing guide's steps five to seven.
batch-*.log
elide-*.log
b3-*.log
b4-*.log
p2-*.log
p2-*.txt
ic-*.log
# Local scratch: `git log` dumps taken while reviewing history.
gitlog.txt
tex-*.log
build/texture_dumps/
# Run artifacts: loader stderr captures and scratch EEPROM images.
run.txt
test.dat
