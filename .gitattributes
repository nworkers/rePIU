# Shell scripts must keep LF endings. This repository is developed on Windows
# with core.autocrlf=true, which otherwise checks them out with CRLF and Linux
# then fails at the shebang with "bash\r: No such file or directory".
*.sh text eol=lf
